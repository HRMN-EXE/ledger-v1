// The JS surface of this module lives in `src/lib/ledger-alarm.ts` inside the
// app, so the rest of the codebase can import it through the `@/` alias like
// any other module. This file only exists so the package resolves.
export {};
